node_modules
dist
coverage
pnpm-lock.yaml

# Локальная конфигурация предпросмотра — в репозиторий не идёт.
.claude/

# Продуктовые и доменные документы форматтер не трогает.
# Он переносит строки и перестраивает вложенность списков — на спеках
# это ломает структуру и создаёт шум в истории. Проверено на UI-SPEC 4.3.
*.md
docs/**/*.md
ux/**
knowledge-base/**
design_handoff_specform/**
market-research/**
*.html
apps/web/data/

# Машинный вывод: манифест библиотеки собирает templates:ingest, JSON-схему —
# stylespec:schema. Форматировать их значит получать шум в дифе при каждой
# пересборке и спорить с генератором о переносах.
packages/kb/data/templates/template_manifest.json
packages/stylespec/schema/stylespec.schema.json

# Библиотека силуэтов — 1605 файлов SVG и PNG. Парсера для них у Prettier
# нет вовсе: он их читает и отбрасывает. На забитой памяти этот обход
# растянул проверку формата с секунд до сорока минут (11.09.2026) и
# выглядел как зависший гейт. Форматировать здесь нечего — пропускаем.
packages/kb/data/templates/
